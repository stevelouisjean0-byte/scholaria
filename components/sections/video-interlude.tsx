// Deprecated: replaced by `components/feature-video.tsx` which uses a
// lite-YouTube embed pattern. Kept as a stub so any straggler imports do not
// break the build.
export function VideoInterlude() {
  return null;
}
